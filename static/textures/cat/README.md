Optional cat coat textures:

- `default.jpg`
- `black.jpg`
- `cream.jpg`

The Three.js runtime sets `texture.flipY = false` and `texture.colorSpace = THREE.SRGBColorSpace` before assigning textures to the GLTF material.
