Place the production cat model here:

- `cat.glb`

Expected animation clip names:

- `idle`
- `petting`

If the model is missing, `assets/js/floscas-cat.js` renders a lightweight procedural glass cat so the site stays functional.
